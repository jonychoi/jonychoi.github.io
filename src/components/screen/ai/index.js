import React from 'react';
import Article from '../../common/article';
import { Flex } from '../../common/base';

export const AI = ({route, setRoute}) => {
    return (
        <Flex>
            <Article />
        </Flex>
    );
};

export default AI;